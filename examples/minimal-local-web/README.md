# Minimal Local Web Example

最小示例：直接复用 framework core，起一个本地 CLI + Web multi-agent app。

```bash
node examples/minimal-local-web/server.js
```

这个示例不加任何自定义：

- 使用默认 runner
- 使用默认模板
- 使用默认 `/api` 前缀
- 使用仓库根目录的 `public/` 作为 demo UI
